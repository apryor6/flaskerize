#!/usr/bin/env python
import sys

from flaskerize import parse, generate, handlers


args = parse(sys.argv[1:])

print('Flaskerizing...')
if args.bundle:
    handlers.handle_bundle(args)


if args.generate:
    handlers.handle_generate(args)


print("Success!")
